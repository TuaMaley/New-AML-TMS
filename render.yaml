services:
  - type: web
    name: aml-tms-tool
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python start.py
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
    autoDeploy: true
